/**
 * 함수
 * js에서 중요한 핵심 개념
 * js의 핵심 개념인 스코프, 실행컨텍스트, 클로저, 생성자 함수에 의한 객체생성, 메서드, this, 프로퍼타입
 * 모듈화 등등 함수와 깊은 관련이있음
 * 
 * 함수는 js를 정확히 이해하고  사용하기 위해서 가장 중요
 * 
 * input > output 이 존재함
 */

//12 - 1
// f(x, y) = x + y
function add(x, y) {
    return x + y;
}

// f (2, 5) = 7
add(2, 5); //7